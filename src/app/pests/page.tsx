import Image from "next/image";import Link from "next/link";import { PESTS } from "@/data/pests.generated";import { DESCRIPTIONS } from "@/data/descriptions";import { withAutoDetails } from "@/lib/pest-gen";export default function PestsPage() {  return (    <main className="max-w-7xl mx-auto px-4 py-10">      <h1 className="text-3xl font-bold mb-6">Pest Library</h1>      <div className="grid gap-6 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4">        {PESTS.map(p => {          const d = withAutoDetails(p, DESCRIPTIONS);          return (            <Link              key={p.slug}              href={`/pest/${p.slug}`}              className="block rounded-2xl shadow p-3 hover:shadow-lg transition"            >              <div className="relative w-full aspect-square overflow-hidden rounded-xl">                <Image                  src={p.image}                  alt={p.title}                  fill                  sizes="(max-width:768px) 100vw, 25vw"                />              </div>              <div className="mt-3 text-center font-medium">{p.title}</div>              <p className="mt-1 text-sm text-gray-600 line-clamp-2">                {d.biology}              </p>            </Link>          );        })}      </div>    </main>  );}